import { BenefitIdPipe } from './benefit-id.pipe';

describe('BenefitIdPipe', () => {
  it('create an instance', () => {
    const pipe = new BenefitIdPipe();
    expect(pipe).toBeTruthy();
  });
});
